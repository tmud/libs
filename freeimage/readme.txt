1. run unzip.bat to unpack FreeImage Base lib.
2. build FreeImageLib (static release lib) by VS2013.
3. build fimage dll project (dll wrapper for FreeImage). 
   Reason : render on windows HDC functions, more simple
